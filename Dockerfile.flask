FROM python:3.11-alpine

WORKDIR /

# Copy requirements or install packages
RUN pip install --no-cache-dir flask pymongo python-dotenv

# Copy application files
COPY . .

# Expose port
EXPOSE 5001

# Start the Flask application
CMD ["python", "-u", "main.py"]
